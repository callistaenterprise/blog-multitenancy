spring:
  main:
    lazy-initialization: true
    banner-mode: "off"
  liquibase:
    enabled: true
    parameters:
      database: ${DB_NAME}
multitenancy:
  master:
    datasource:
      url: ${DB_URL}
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}
